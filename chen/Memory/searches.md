# חן — Search Memory Log

This file logs every web search חן performs. Used for deduplication: before each new search, חן greps this file to avoid re-fetching content already retrieved within the last ~30 days.

**Format per entry:** Each search is a top-level `## YYYY-MM-DD HH:MM | <topic>` section, followed by structured metadata. New entries are appended at the bottom.

---

<!-- Entries below this line. New entries appended chronologically. -->
